info:
  name: addPessoa
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/pessoas
  body:
    type: json
    data: |-
      {
        "nome": "Matheus Zorzete",
        "genero": "Masculino",
        "data_nascimento": "2003-08-09",
        "data_ingresso": "2026-03-22"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
